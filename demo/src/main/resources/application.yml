spring:
  application:
    name: BFBManagement
    
  datasource:
    url: jdbc:h2:mem:bfbdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
        
  springdoc:
    api-docs:
      path: /v3/api-docs
      enabled: true
    swagger-ui:
      path: /swagger-ui.html
      enabled: true
      try-it-out-enabled: true
      operations-sorter: alpha
      tags-sorter: alpha
      display-request-duration: true
      doc-expansion: none
    show-actuator: false

bfb:
  scheduling:
    mark-late-job:
      enabled: true
      cron: "0 0 2 * * ?"

logging:
  level:
    com.bfb: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springdoc: DEBUG
    org.springframework.web: DEBUG
    org.postgresql: DEBUG  # ⬅️ Ajouté pour voir les logs PostgreSQL
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"